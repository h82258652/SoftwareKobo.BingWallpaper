<Page x:Class="SoftwareKobo.BingWallpaper.WindowsPhone.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SoftwareKobo.BingWallpaper.WindowsPhone"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:Microsoft.Phone.Controls"
      xmlns:toolkit="using:WinRTXamlToolkit.Controls"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Hub Header="必应壁纸">
            <Hub.Background>
                <ImageBrush Stretch="UniformToFill"
                            ImageSource="http://cn.bing.com/az/hprichbg/rb//BohemianWaxwings_ZH-CN11501414267_1920x1080.jpg" />
            </Hub.Background>
            <HubSection Header="壁纸">
                <DataTemplate>
                    <Grid>
                        <ListView>
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsStackPanel />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                        </ListView>
                        <Button Click="ButtonBase_OnClick" />
                    </Grid>
                </DataTemplate>
            </HubSection>
            <HubSection Header="设置">
                <DataTemplate>
                    <Grid>
                        <StackPanel>
                            <ComboBox Header="分辨率" ItemsSource="{Binding Path=WallpaperSizes}" DisplayMemberPath="Key" SelectedValuePath="Value"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </HubSection>
            <HubSection Header="关于">
                <DataTemplate>
                    <Grid />
                </DataTemplate>
            </HubSection>
        </Hub>
    </Grid>
</Page>